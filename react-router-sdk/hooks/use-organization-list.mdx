---
title: 'useOrganizationList()'
description: 'A headless hook for retrieving and managing the array of organizations a user belongs to.'
---

# `useOrganizationList()`

In B2B applications, a single user profile often belongs to multiple distinct tenants (organizations). The `useOrganizationList()` hook provides a straightforward, paginated interface to fetch, list, and interact with every organization the current user has access to.

It exposes the exact primitives required to build a bespoke `<OrganizationSwitcher />` dropdown or an "Accept Invitations" dashboard page.

## Import

```tsx
import { useOrganizationList } from "@wacht/react-router";
```

## Anatomy

<ResponseField name="loading" type="boolean">
  Boolean indicating network boundary readiness.
</ResponseField>

<ResponseField name="organizations" type="Organization[]">
  List of actual Wacht Organization objects the user is a member of.
</ResponseField>

<ResponseField name="error" type="Error | null">
  Any error encountered during fetching.
</ResponseField>

<ResponseField name="refetch" type="() => Promise<void>">
  Manually triggers a re-fetch of the organization list from the server.
</ResponseField>

<ResponseField name="Organization Management" type="Functions">
  <Expandable title="Organization functions">
    <ResponseField name="createOrganization" type="(payload: NewOrganization) => Promise<ApiResult<unknown>>">
      Creates a new organization.
    </ResponseField>
    <ResponseField name="updateOrganization" type="(org: Organization, payload: OrganizationUpdate) => Promise<ApiResult<Organization>>">
      Updates basic properties of a specific organization.
    </ResponseField>
    <ResponseField name="deleteOrganization" type="(org: Organization) => Promise<ApiResult<void>>">
      Deletes an organization completely.
    </ResponseField>
    <ResponseField name="leaveOrganization" type="(org: Organization) => Promise<ApiResult<void>>">
      Leaves a specific organization.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Member & Role Management" type="Functions">
  <Expandable title="Member functions">
    <ResponseField name="getOrganizationMembers" type="(org: Organization, params?: { page: number; limit: number; search?: string }) => Promise<ApiResult<PaginatedResponse<OrganizationMembership[]>>>">
      Fetches paginated list of members for an organization.
    </ResponseField>
    <ResponseField name="removeOrganizationMember" type="(org: Organization, member: OrganizationMembership) => Promise<void>">
      Removes a member from the organization.
    </ResponseField>
    <ResponseField name="getOrganizationRoles" type="(org: Organization) => Promise<ApiResult<OrganizationRole[]>>">
      Retrieves all roles available in the organization.
    </ResponseField>
    <ResponseField name="addRole" type="(org: Organization, role: RoleCreate) => Promise<ApiResult<OrganizationRole>>">
      Creates a new custom role in the organization.
    </ResponseField>
    <ResponseField name="removeOrganizationRoles" type="(org: Organization, role: OrganizationRole) => Promise<void>">
      Deletes a role from the organization.
    </ResponseField>
    <ResponseField name="addRoleToOrganizationMember" type="(org: Organization, member: OrganizationMembership, role: OrganizationRole) => Promise<ApiResult<OrganizationMembership>>">
      Assigns a role to a specific member.
    </ResponseField>
    <ResponseField name="removeRoleFromOrganizationMember" type="(org: Organization, member: OrganizationMembership, role: OrganizationRole) => Promise<ApiResult<OrganizationMembership>>">
      Removes a role from a specific member.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Invitation Management" type="Functions">
  <Expandable title="Invitation functions">
    <ResponseField name="getOrganizationInvitations" type="(org: Organization) => Promise<ApiResult<OrganizationInvitation[]>>">
      Fetches pending invitations for an organization.
    </ResponseField>
    <ResponseField name="inviteOrganizationMember" type="(org: Organization, payload: OrganizationInvitationPayload) => Promise<ApiResult<OrganizationInvitation>>">
      Invites a new member to the organization via email.
    </ResponseField>
    <ResponseField name="resendOrganizationInvitation" type="(org: Organization, invitation: OrganizationInvitation) => Promise<ApiResult<OrganizationInvitation>>">
      Resends an outstanding invitation email.
    </ResponseField>
    <ResponseField name="discardOrganizationInvitation" type="(org: Organization, invitation: OrganizationInvitation) => Promise<ApiResult<OrganizationInvitation>>">
      Revokes a pending invitation.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Domain Management" type="Functions">
  <Expandable title="Domain functions">
    <ResponseField name="getOrganizationDomains" type="(org: Organization) => Promise<ApiResult<OrganizationDomain[]>>">
      Retrieves domains linked to the organization.
    </ResponseField>
    <ResponseField name="addOrganizationDomain" type="(org: Organization, domain: NewDomain) => Promise<ApiResult<OrganizationDomain>>">
      Adds a new custom domain for organizational identity.
    </ResponseField>
    <ResponseField name="verifyOrganizationDomain" type="(org: Organization, domain: OrganizationDomain) => Promise<ApiResult<OrganizationDomain>>">
      Triggers DNS verification for an added domain.
    </ResponseField>
    <ResponseField name="removeOrganizationDomain" type="(org: Organization, domain: OrganizationDomain) => Promise<ApiResult<OrganizationDomain>>">
      Removes a custom domain.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Enterprise Connections (SSO)" type="Functions">
  <Expandable title="SSO functions">
    <ResponseField name="getEnterpriseConnections" type="(org: Organization) => Promise<ApiResult<EnterpriseConnection[]>>">
      Lists active SAML/OIDC enterprise connections.
    </ResponseField>
    <ResponseField name="createEnterpriseConnection" type="(org: Organization, payload: CreateEnterpriseConnectionPayload) => Promise<ApiResult<EnterpriseConnection>>">
      Establishes a new enterprise SSO connection.
    </ResponseField>
    <ResponseField name="updateEnterpriseConnection" type="(org: Organization, connectionId: string, payload: UpdateEnterpriseConnectionPayload) => Promise<ApiResult<EnterpriseConnection>>">
      Updates enterprise SSO settings.
    </ResponseField>
    <ResponseField name="testEnterpriseConnectionConfig" type="(org: Organization, payload: TestPayload) => Promise<ApiResult<TestResult>>">
      Tests SSO capabilities of a provided configuration.
    </ResponseField>
    <ResponseField name="testEnterpriseConnection" type="(org: Organization, connectionId: string) => Promise<ApiResult<TestResult>>">
      Tests an existing enterprise connection.
    </ResponseField>
    <ResponseField name="deleteEnterpriseConnection" type="(org: Organization, connectionId: string) => Promise<void>">
      Removes an enterprise connection.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="SCIM Directory Sync" type="Functions">
  <Expandable title="SCIM functions">
    <ResponseField name="getSCIMToken" type="(org: Organization, connectionId: string) => Promise<ApiResult<SCIMTokenInfo>>">
      Retrieves the active SCIM provisioning token information.
    </ResponseField>
    <ResponseField name="generateSCIMToken" type="(org: Organization, connectionId: string) => Promise<ApiResult<SCIMTokenInfo>>">
      Generates a new SCIM provisioning token.
    </ResponseField>
    <ResponseField name="revokeSCIMToken" type="(org: Organization, connectionId: string) => Promise<void>">
      Revokes the SCIM provisioning token instantly.
    </ResponseField>
  </Expandable>
</ResponseField>

## Usage: Building an Invites Page

One of the most powerful uses of this hook is constructing an interface to let users view and accept their pending invitations to join other teams.

```tsx
import { useOrganizationList } from "@wacht/react-router";

export function UserOrganizations() {
  const { loading, organizations } = useOrganizationList();

  if (loading) return <div>Loading organizations...</div>;

  if (!organizations || organizations.length === 0) {
    return <p>You have no active organizations.</p>;
  }

  return (
    <ul className="divide-y divide-zinc-200">
      {organizations.map((org) => (
        <li key={org.id} className="py-4 flex items-center justify-between">
          <p className="font-medium text-zinc-900">
            {org.name}
          </p>
        </li>
      ))}
    </ul>
  );
}
```

## Interacting With Organizations

The hook provides numerous functions to manipulate organizations if the user holds required permissions, such as:
- `createOrganization(payload)`
- `updateOrganization(org, payload)`
- `leaveOrganization(org)`
- `getOrganizationMembers(org)`
