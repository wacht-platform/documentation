---
title: 'useActiveOrganization()'
description: 'A React hook that exposes the metadata and Role-Based Access Control (RBAC) permissions for the currently selected tenant.'
---

# `useActiveOrganization()`

The `useActiveOrganization()` hook is arguably the most critical hook for maintaining data isolation in a B2B application. It retrieves the specific `Organization` object that the user has currently selected within their session context.

It provides immediate access to the organization's unique ID, its custom metadata, and—most importantly—provides rigorous utility functions for enforcing authorization gates on the frontend.

## Import

```tsx
import { useActiveOrganization } from "@wacht/react-router";
```

## Anatomy

<ResponseField name="loading" type="boolean">
  Boolean indicating SDK readiness.
</ResponseField>

<ResponseField name="activeOrganization" type="Organization | null">
  The complete initialized Organization profile of the currently active tenant.
</ResponseField>

<ResponseField name="activeMembership" type="OrganizationMembershipWithOrganization | null">
  The current user's specific membership context within the active organization.
</ResponseField>

<ResponseField name="error" type="Error | null">
  Any error encountered during organization initialization.
</ResponseField>

<ResponseField name="refetch" type="() => Promise<void>">
  Manually triggers a re-fetch of the active organization and membership data.
</ResponseField>

<ResponseField name="Organization Management" type="Functions">
  <Expandable title="Organization functions">
    <ResponseField name="updateOrganization" type="(payload: OrganizationUpdate) => Promise<ApiResult<Organization>>">
      Updates basic properties of the active organization.
    </ResponseField>
    <ResponseField name="leave" type="() => Promise<ApiResult<void>>">
      Leaves the currently active organization.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Member & Role Management" type="Functions">
  <Expandable title="Member functions">
    <ResponseField name="getMembers" type="(params?: { page: number; limit: number; search?: string }) => Promise<ApiResult<PaginatedResponse<OrganizationMembership[]>>>">
      Fetches paginated list of members for the active organization.
    </ResponseField>
    <ResponseField name="removeMember" type="(member: OrganizationMembership) => Promise<void>">
      Removes a member from the active organization.
    </ResponseField>
    <ResponseField name="getRoles" type="() => Promise<ApiResult<OrganizationRole[]>>">
      Retrieves all roles available in the organization.
    </ResponseField>
    <ResponseField name="removeRole" type="(role: OrganizationRole) => Promise<void>">
      Deletes a role from the active organization.
    </ResponseField>
    <ResponseField name="addMemberRole" type="(member: OrganizationMembership, role: OrganizationRole) => Promise<ApiResult<OrganizationMembership>>">
      Assigns a role to a specific member within the active organization.
    </ResponseField>
    <ResponseField name="removeMemberRole" type="(member: OrganizationMembership, role: OrganizationRole) => Promise<ApiResult<OrganizationMembership>>">
      Removes a role from a specific member.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Invitation Management" type="Functions">
  <Expandable title="Invitation functions">
    <ResponseField name="getInvitations" type="() => Promise<ApiResult<OrganizationInvitation[]>>">
      Fetches pending invitations for the active organization.
    </ResponseField>
    <ResponseField name="inviteMember" type="(payload: OrganizationInvitationPayload) => Promise<ApiResult<OrganizationInvitation>>">
      Invites a new member to the active organization via email.
    </ResponseField>
    <ResponseField name="resendInvitation" type="(invitation: OrganizationInvitation) => Promise<ApiResult<OrganizationInvitation>>">
      Resends an outstanding invitation email.
    </ResponseField>
    <ResponseField name="discardInvitation" type="(invitation: OrganizationInvitation) => Promise<ApiResult<OrganizationInvitation>>">
      Revokes a pending invitation.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Domain Management" type="Functions">
  <Expandable title="Domain functions">
    <ResponseField name="getDomains" type="() => Promise<ApiResult<OrganizationDomain[]>>">
      Retrieves domains linked to the active organization.
    </ResponseField>
    <ResponseField name="addDomain" type="(domain: NewDomain) => Promise<ApiResult<OrganizationDomain>>">
      Adds a new custom domain for organizational identity.
    </ResponseField>
    <ResponseField name="verifyDomain" type="(domain: OrganizationDomain) => Promise<ApiResult<OrganizationDomain>>">
      Triggers DNS verification for an added domain.
    </ResponseField>
    <ResponseField name="removeDomain" type="(domain: OrganizationDomain) => Promise<ApiResult<OrganizationDomain>>">
      Removes a custom domain.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Enterprise Connections (SSO)" type="Functions">
  <Expandable title="SSO functions">
    <ResponseField name="getEnterpriseConnections" type="() => Promise<ApiResult<EnterpriseConnection[]>>">
      Lists active SAML/OIDC enterprise connections for the active organization.
    </ResponseField>
    <ResponseField name="createEnterpriseConnection" type="(payload: CreateEnterpriseConnectionPayload) => Promise<ApiResult<EnterpriseConnection>>">
      Establishes a new enterprise SSO connection.
    </ResponseField>
    <ResponseField name="updateEnterpriseConnection" type="(connectionId: string, payload: UpdateEnterpriseConnectionPayload) => Promise<ApiResult<EnterpriseConnection>>">
      Updates enterprise SSO settings.
    </ResponseField>
    <ResponseField name="testEnterpriseConnectionConfig" type="(payload: TestPayload) => Promise<ApiResult<TestResult>>">
      Tests SSO capabilities of a provided configuration.
    </ResponseField>
    <ResponseField name="testEnterpriseConnection" type="(connectionId: string) => Promise<ApiResult<TestResult>>">
      Tests an existing enterprise connection.
    </ResponseField>
    <ResponseField name="deleteEnterpriseConnection" type="(connectionId: string) => Promise<void>">
      Removes an enterprise connection.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="SCIM Directory Sync" type="Functions">
  <Expandable title="SCIM functions">
    <ResponseField name="getSCIMToken" type="(connectionId: string) => Promise<ApiResult<SCIMTokenInfo>>">
      Retrieves the active SCIM provisioning token information.
    </ResponseField>
    <ResponseField name="generateSCIMToken" type="(connectionId: string) => Promise<ApiResult<SCIMTokenInfo>>">
      Generates a new SCIM provisioning token.
    </ResponseField>
    <ResponseField name="revokeSCIMToken" type="(connectionId: string) => Promise<void>">
      Revokes the SCIM provisioning token instantly.
    </ResponseField>
  </Expandable>
</ResponseField>

## Usage: Accessing Basic Org Data

You can use the `organization` object to stamp your UI with tenant-specific branding.

```tsx
import { useActiveOrganization } from "@wacht/react-router";

export function TeamDashboardHeader() {
  const { loading, activeOrganization } = useActiveOrganization();

  if (loading) return null;

  // Fallback UI if the user hasn't selected a team
  if (!activeOrganization) {
    return <h1>Personal Dashboard</h1>;
  }

  return (
    <header className="flex items-center gap-3">
      {activeOrganization.image && (
        <img src={activeOrganization.image as string} className="w-8 h-8 rounded-md" alt="Logo" />
      )}
      <h1 className="text-2xl font-bold tracking-tight text-zinc-900">
        {activeOrganization.name} Workspace
      </h1>
    </header>
  );
}
```
## Client-Side Authorization (RBAC)

When building complex interfaces, certain destructive actions (like deleting a project or changing billing details) should only be accessible to users with specific roles. 

You can leverage the `activeMembership.roles` array or the global JWT session permissions to evaluate a user's rights against the active organizational context.

> [!WARNING]
> Client-side RBAC checks are excellent for UI/UX (hiding buttons or panels), but they are **not** a secure barrier. You must always re-verify permissions on your backend server by executing `RequireAuth` before executing destructive mutations to the database.
