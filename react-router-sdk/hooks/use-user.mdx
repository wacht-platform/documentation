---
title: 'useUser()'
description: 'Access the authenticated User context, metadata, and profile modification methods.'
---

# `useUser()`

The `useUser()` hook exposes the primary `User` data construct, facilitating access to profile parameters, associated email identifiers, and custom public metadata attributes. Additionally, the hook provides the interface methods necessary to mutate user parameters dynamically from the client application.

## Hook Import

```tsx
import { useUser } from "@wacht/react-router";
```

## Hook Interface

<ResponseField name="loading" type="boolean">
  Indicates whether the SDK is actively executing a network request to synchronize the user identity context.
</ResponseField>

<ResponseField name="user" type="CurrentUser & { refetch: () => void } | null">
  The active `CurrentUser` construct, alongside a `refetch` method to force invalidation. If the underlying session is unauthenticated, this property evaluates to `null`.
</ResponseField>

<ResponseField name="error" type="Error | null">
  Captures network failures or processing exceptions encountered during the fetch operation.
</ResponseField>

---

<ResponseField name="Profile Operations" type="Functions">
  <Expandable title="Profile Modification Interface">
    <ResponseField name="updateProfile" type="(data: ProfileUpdateData) => Promise<ApiResult<CurrentUser>>">
      <Expandable title="Modify User Attributes">
        <ParamField path="data" type="ProfileUpdateData">
          The subset of profile attributes designated for modification.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<CurrentUser>>">
          Resolves with the serialized updated user construct.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updateProfilePicture" type="(file: File) => Promise<ApiResult<unknown>>">
      <Expandable title="Modify Profile Asset">
        <ParamField path="file" type="File">
          The binary representation of the image asset.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful upload and processing of the profile asset.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updatePassword" type="(currentPassword: string, newPassword: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Modify Credential Vector">
        <ParamField path="currentPassword" type="string">
          The user's current validation credential.
        </ParamField>
        <ParamField path="newPassword" type="string">
          The newly designated validation credential.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful credential modification payload processing.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="removePassword" type="(currentPassword: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Remove Credential Vector">
        <ParamField path="currentPassword" type="string">
          The user's current validation credential.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful credential invalidation and removal securely.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteAccount" type="(password: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Destroy User Record">
        <ParamField path="password" type="string">
          The validation credential confirming intent.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful invocation of the account destruction process.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Email Identifier Operations" type="Functions">
  <Expandable title="Email Verification Interface">
    <ResponseField name="getEmailAddresses" type="() => Promise<ApiResult<UserEmailAddress[]>>">
      <Expandable title="Retrieve Identifier Scope">
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress[]>>">
          Returns an array detailing the email identifiers mapped to this user instance.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="getEmailAddress" type="(id: string) => Promise<ApiResult<UserEmailAddress>>">
      <Expandable title="Retrieve Specific Identifier">
        <ParamField path="id" type="string">
          The system identifier of the target address record.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress>>">
          Resolves with the serialized details of the requested email record.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="createEmailAddress" type="(email: string) => Promise<ApiResult<UserEmailAddress>>">
      <Expandable title="Initialize Additional Identifier">
        <ParamField path="email" type="string">
          The target address to append to the user sequence.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress>>">
          Resolves with the details of the newly instantiated unverified record.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteEmailAddress" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Remove Defined Identifier">
        <ParamField path="id" type="string">
          The system identifier of the address record designated for removal.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful extraction of the identity mapping.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="prepareEmailVerification" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Initiate Identifier Verification">
        <ParamField path="id" type="string">
          The system identifier of the address to verify.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon transmission of the verification challenge matrix.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="attemptEmailVerification" type="(id: string, otp: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Submit Verification Challenge">
        <ParamField path="id" type="string">
          The system identifier of the address being verified.
        </ParamField>
        <ParamField path="otp" type="string">
          The client-submitted challenge response sequence.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful backend validation of the identity tie.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="makeEmailPrimary" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Promote Primary Identifier">
        <ParamField path="id" type="string">
          The system identifier of the address to designate as root.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon modification of the user routing rules logic.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Phone Identifier Operations" type="Functions">
  <Expandable title="Phone Verification Interface">
    <ResponseField name="createPhoneNumber" type="(phone_number: string, country_code: string) => Promise<ApiResult<UserPhoneNumber>>">
      <Expandable title="Initialize Telephony Identifier">
        <ParamField path="phone_number" type="string">
          The telephony address to append.
        </ParamField>
        <ParamField path="country_code" type="string">
          The internationally defined prefix zone mapping.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserPhoneNumber>>">
          Resolves with the unverified record parameters.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deletePhoneNumber" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Remove Telephony Identifier">
        <ParamField path="id" type="string">
          The system identifier mapping to the configured record.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful separation of the identifier mapping.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preparePhoneVerification" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Initiate Telephony Verification">
        <ParamField path="id" type="string">
          The system identifier.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon transmission of the SMS verification sequence.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="attemptPhoneVerification" type="(id: string, otp: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Submit Telephony Challenge">
        <ParamField path="id" type="string">
          The system identifier.
        </ParamField>
        <ParamField path="otp" type="string">
          The client-provided challenge parameter response.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon successful validation matching the generated backend sequence.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="makePhonePrimary" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Promote Primary Telephony Identifier">
        <ParamField path="id" type="string">
          The system identifier.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves upon mapping update logic.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Multi-Factor (TOTP) Operations" type="Functions">
  <Expandable title="TOTP Authenticator Interface">
    <ResponseField name="setupAuthenticator" type="() => Promise<ApiResult<UserAuthenticator>>">
      <Expandable title="Initialize TOTP Profile">
        <ResponseField name="returns" type="Promise<ApiResult<UserAuthenticator>>">
          Resolves with the cryptographic seed material and URI definitions mapping the initialization vector.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="verifyAuthenticator" type="(id: string, codes: string[]) => Promise<ApiResult<unknown>>">
      <Expandable title="Validate TOTP Interlock">
        <ParamField path="id" type="string">
          The designated identification string.
        </ParamField>
        <ParamField path="codes" type="string[]">
          An initial set of validity codes demonstrating time-aligned configuration.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves linking the validation logic upon sequence confirmation natively.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteAuthenticator" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Disconnect TOTP Factor">
        <ParamField path="id" type="string">
          The target identifier payload to dismantle.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          Resolves on successful severing of the TOTP challenge logic logic.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Recovery Context Methods" type="Functions">
  <Expandable title="Backup Sequence Recovery">
    <ResponseField name="generateBackupCodes" type="() => Promise<ApiResult<string[]>>">
      <Expandable title="Initialize Recovery Block">
        <ResponseField name="returns" type="Promise<ApiResult<string[]>>">
          Returns an array representing the newly minted static backup code payloads natively mapping to configuration parameters natively.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="regenerateBackupCodes" type="() => Promise<ApiResult<string[]>>">
      <Expandable title="Cycle Recovery Block">
        <ResponseField name="returns" type="Promise<ApiResult<string[]>>">
          Replaces and returns a substitute backup code array sequence while revoking prior material allocations dynamically.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Provider & Passkey Implementations" type="Functions">
  <Expandable title="Advanced Interface Connectors">
    <ResponseField name="connectSocialAccount" type="(params: { provider: string; redirectUri?: string }) => Promise<ApiResult<{ oauth_url: string }>>">
      <Expandable title="Initialize Provider Handshake">
        <ParamField path="params.provider" type="string">
          The target authorization server (e.g., 'google').
        </ParamField>
        <ParamField path="params.redirectUri" type="string">
          Optional local domain callback URI mapped for validation processing context flows natively via processing.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<{ oauth_url: string }>>">
          Generates the specific URL sequence routing to the authorization provider interface natively mapping directly via redirect initialization sequence mapping payload securely natively locally directly natively locally directly natively locally natively.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="getPasskeys" type="() => Promise<ApiResult<UserPasskey[]>>">
      <Expandable title="Retrieve Security Keys">
        <ResponseField name="returns" type="Promise<ApiResult<UserPasskey[]>>">
          Resolves yielding active cryptographic key structures mapped dynamically natively locally natively.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Implementation Guidelines

### Accessing Public Metadata Payload

Standard implementation architectures persist domain-specific configuration elements (such as UI preferences) inside the User Object's `publicMetadata` envelope. These attributes are securely dispatched to the client context post-authentication sequence natively.

```tsx
import { useUser } from "@wacht/react-router";

export function InterfaceConfigurationManager() {
  const { loading, user } = useUser();

  if (loading || !user) return null;

  const currentRoleConstraint = user.publicMetadata.role_constraint as string | undefined;

  return (
    <div className="text-sm border border-gray-200 p-4 rounded bg-white">
      System rendering mapping block identifier sequence string logic dynamically resolving parameter locally explicitly: 
      <span className="font-semibold text-gray-900 ml-2">
        {currentRoleConstraint || "UNASSIGNED_LOGIC_BLOCK_FALLBACK_DEFAULT"}
      </span>
    </div>
  );
}
```

### Mutating Account Properties

Attributes allocated to the `user` reference natively supply binding logic for direct remote record modification mapping parameters effectively directly matching specific requirements effectively effectively explicit sequence logic mappings implicitly updating contexts iteratively updating records securely. 

```tsx
import { useUser } from "@wacht/react-router";

export function ModifySequenceComponent() {
  const { user } = useUser();

  const handleUpdateTransaction = async () => {
    if (!user) return;
    try {
      await user.update({ firstName: "Jonathan" });
    } catch (err) {
      console.error("Mutation modification constraint enforcement triggered exception: ", err);
    }
  };

  return (
    <button onClick={handleUpdateTransaction} className="border p-2 bg-gray-50 uppercase text-xs">
      Submit Modification Sequence
    </button>
  );
}
```
