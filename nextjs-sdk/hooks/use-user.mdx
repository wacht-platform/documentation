---
title: 'useUser()'
description: 'A React hook that provides access to the currently authenticated User context and metadata.'
---

# `useUser()`

The `useUser()` hook exposes the active `User` object, allowing you to seamlessly integrate the user's profile information—such as their first name, last name, profile image, and custom public metadata—directly into your frontend interface.

## Import

```tsx
import { useUser } from "@wacht/nextjs";
```

## Anatomy

<ResponseField name="loading" type="boolean">
  Indicates whether the SDK is actively verifying the user's identity asynchronously.
</ResponseField>

<ResponseField name="user" type="CurrentUser & { refetch: () => void } | null">
  The active `CurrentUser` object (including a `refetch` method to manually reload it), or `null` if the user is completely logged out.
</ResponseField>

<ResponseField name="error" type="Error | null">
  Any error encountered during the user fetch operation.
</ResponseField>

<ResponseField name="Profile Management Methods" type="Functions">
  <Expandable title="Profile functions">
    <ResponseField name="updateProfile" type="(data: ProfileUpdateData) => Promise<ApiResult<CurrentUser>>">
      <Expandable title="Update profile">
        <ParamField path="data" type="ProfileUpdateData">
          The profile fields to update.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<CurrentUser>>">
          The updated user profile.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updateProfilePicture" type="(file: File) => Promise<ApiResult<unknown>>">
      <Expandable title="Update profile picture">
        <ParamField path="file" type="File">
          The image file to upload as the profile picture.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the profile picture update.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updatePassword" type="(currentPassword: string, newPassword: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Update password">
        <ParamField path="currentPassword" type="string">
          The user's current password.
        </ParamField>
        <ParamField path="newPassword" type="string">
          The user's new password.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the password update.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="removePassword" type="(currentPassword: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Remove password">
        <ParamField path="currentPassword" type="string">
          The user's current password.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the password removal.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteAccount" type="(password: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Delete account">
        <ParamField path="password" type="string">
          The user's password to confirm account deletion.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the account deletion.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Email Management Methods" type="Functions">
  <Expandable title="Email functions">
    <ResponseField name="getEmailAddresses" type="() => Promise<ApiResult<UserEmailAddress[]>>">
      <Expandable title="Get email addresses">
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress[]>>">
          List of email addresses linked to the user.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="getEmailAddress" type="(id: string) => Promise<ApiResult<UserEmailAddress>>">
      <Expandable title="Get specific email address">
        <ParamField path="id" type="string">
          The ID of the email address to fetch.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress>>">
          The specific email address.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="createEmailAddress" type="(email: string) => Promise<ApiResult<UserEmailAddress>>">
      <Expandable title="Create email address">
        <ParamField path="email" type="string">
          The new email address to link.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserEmailAddress>>">
          The newly created email address.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteEmailAddress" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Delete email address">
        <ParamField path="id" type="string">
          The ID of the email address to remove.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the deletion.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="prepareEmailVerification" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Prepare email verification">
        <ParamField path="id" type="string">
          The ID of the email address to verify.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of dispatching the verification code.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="attemptEmailVerification" type="(id: string, otp: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Attempt email verification">
        <ParamField path="id" type="string">
          The ID of the email address being verified.
        </ParamField>
        <ParamField path="otp" type="string">
          The verification code sent to the email address.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the verification attempt.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="makeEmailPrimary" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Make email primary">
        <ParamField path="id" type="string">
          The ID of the email address to set as primary.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of setting the primary email.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Phone Management Methods" type="Functions">
  <Expandable title="Phone functions">
    <ResponseField name="createPhoneNumber" type="(phone_number: string, country_code: string) => Promise<ApiResult<UserPhoneNumber>>">
      <Expandable title="Create phone number">
        <ParamField path="phone_number" type="string">
          The new phone number to link.
        </ParamField>
        <ParamField path="country_code" type="string">
          The ISO country code for the phone number.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<UserPhoneNumber>>">
          The newly created phone number.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deletePhoneNumber" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Delete phone number">
        <ParamField path="id" type="string">
          The ID of the phone number to remove.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the deletion.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preparePhoneVerification" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Prepare phone verification">
        <ParamField path="id" type="string">
          The ID of the phone number to verify.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of dispatching the verification code.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="attemptPhoneVerification" type="(id: string, otp: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Attempt phone verification">
        <ParamField path="id" type="string">
          The ID of the phone number being verified.
        </ParamField>
        <ParamField path="otp" type="string">
          The verification code sent to the phone number.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the verification attempt.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="makePhonePrimary" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Make phone primary">
        <ParamField path="id" type="string">
          The ID of the phone number to set as primary.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of setting the primary phone number.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Authenticator (TOTP) Methods" type="Functions">
  <Expandable title="Authenticator functions">
    <ResponseField name="setupAuthenticator" type="() => Promise<ApiResult<UserAuthenticator>>">
      <Expandable title="Setup authenticator">
        <ResponseField name="returns" type="Promise<ApiResult<UserAuthenticator>>">
          The setup details (including secret and QR code URI) for a new TOTP authenticator.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="verifyAuthenticator" type="(id: string, codes: string[]) => Promise<ApiResult<unknown>>">
      <Expandable title="Verify authenticator">
        <ParamField path="id" type="string">
          The ID of the authenticator being verified.
        </ParamField>
        <ParamField path="codes" type="string[]">
          An array of codes generated by the authenticator app to verify setup.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of verifying the authenticator.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deleteAuthenticator" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Delete authenticator">
        <ParamField path="id" type="string">
          The ID of the authenticator to remove.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of removing the authenticator.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Backup Code Methods" type="Functions">
  <Expandable title="Backup Code functions">
    <ResponseField name="generateBackupCodes" type="() => Promise<ApiResult<string[]>>">
      <Expandable title="Generate backup codes">
        <ResponseField name="returns" type="Promise<ApiResult<string[]>>">
          A list of newly generated MFA backup codes.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="regenerateBackupCodes" type="() => Promise<ApiResult<string[]>>">
      <Expandable title="Regenerate backup codes">
        <ResponseField name="returns" type="Promise<ApiResult<string[]>>">
          A list of newly generated MFA backup codes (invalidating previous ones).
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Social & SSO Methods" type="Functions">
  <Expandable title="Social functions">
    <ResponseField name="connectSocialAccount" type="(params: { provider: string; redirectUri?: string }) => Promise<ApiResult<{ oauth_url: string }>>">
      <Expandable title="Connect social account">
        <ParamField path="params.provider" type="string">
          The social provider to connect (e.g., 'google', 'github').
        </ParamField>
        <ParamField path="params.redirectUri" type="string">
          Optional URI to redirect back to after connecting.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<{ oauth_url: string }>>">
          The OAuth flow initialization URL to redirect the user to.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disconnectSocialConnection" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Disconnect social connection">
        <ParamField path="id" type="string">
          The ID of the social connection to disconnect.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the disconnection.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="Passkey Methods" type="Functions">
  <Expandable title="Passkey functions">
    <ResponseField name="getPasskeys" type="() => Promise<ApiResult<UserPasskey[]>>">
      <Expandable title="Get passkeys">
        <ResponseField name="returns" type="Promise<ApiResult<UserPasskey[]>>">
          List of registered WebAuthn passkeys for the user.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="registerPasskey" type="(name?: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Register passkey">
        <ParamField path="name" type="string">
          An optional, human-readable name for the new passkey.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the native WebAuthn registration flow.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="renamePasskey" type="(id: string, name: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Rename passkey">
        <ParamField path="id" type="string">
          The ID of the passkey to rename.
        </ParamField>
        <ParamField path="name" type="string">
          The new name for the passkey.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the renaming operation.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deletePasskey" type="(id: string) => Promise<ApiResult<unknown>>">
      <Expandable title="Delete passkey">
        <ParamField path="id" type="string">
          The ID of the passkey to remove.
        </ParamField>
        <ResponseField name="returns" type="Promise<ApiResult<unknown>>">
          The result of the deletion operation.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Usage: Personalized Greetings

The hallmark of a great user experience is personalization. You can easily access the `user` object to populate dashboard headers or welcome messages.

```tsx
import { useUser } from "@wacht/nextjs";

export function WelcomeBanner() {
  const { loading, user } = useUser();

  if (loading || !user) {
    return null; // Fail silently if not logged in
  }

  return (
    <div className="bg-blue-50 border border-blue-100 rounded-lg p-6 mb-8">
      <h2 className="text-xl font-bold text-blue-900 tracking-tight">
        Welcome back, {user.firstName || "friend"} {/* Fallback if no first name */}
      </h2>
      <p className="text-blue-700 mt-1">
        Your primary email is currently mapped to {user.primaryEmailAddress}.
      </p>
    </div>
  );
}
```

## Accessing Custom Metadata

Wacht allows you to attach custom JSON metadata to individual User objects from the Dashboard or Backend API. This is incredibly useful for storing application-specific state (like a user's theme preference, onboarding status, or stripe customer ID) directly on the identity layer.

If a piece of metadata is flagged as `public`, it becomes securely accessible directly from the frontend React SDK via this hook:

```tsx
import { useUser } from "@wacht/nextjs";

export function ThemeController() {
  const { loading, user } = useUser();

  if (loading || !user) return null;

  // Access the custom "publicMetadata" object securely attached via the backend
  const userPreferredTheme = user.publicMetadata.theme || "light";

  return (
    <div className="text-sm text-zinc-500 mt-4">
      System rendering in module: <span className="font-bold">{userPreferredTheme}</span>
    </div>
  );
}
```

## Updating User Data

The `user` object returned by this hook is a rich instance possessing its own suite of update functions. You can programmatically update a user's profile directly from the client (provided the updates conform to your deployment's configuration policies).

```tsx
const handleUpdateName = async () => {
  try {
    const updatedUser = await user.update({ firstName: "Jonathan" });
    console.log("Profile successfully updated!", updatedUser);
  } catch (err) {
    console.error("Failed to update profile:", err);
  }
};
```
