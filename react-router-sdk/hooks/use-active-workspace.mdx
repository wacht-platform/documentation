---
title: useActiveWorkspace
description: Access and manage the currently active workspace.
---

# useActiveWorkspace

The `useActiveWorkspace` hook provides access to the currently active workspace and methods to manage it.

## Return Value

<ResponseField name="activeWorkspace" type="Workspace | null">
  The active workspace object
</ResponseField>

<ResponseField name="activeMembership" type="WorkspaceMembership | null">
  User's membership in active workspace
</ResponseField>

<ResponseField name="loading" type="boolean">
  Whether data is loading
</ResponseField>

<ResponseField name="error" type="Error | null">
  Any error that occurred
</ResponseField>

<ResponseField name="refetch" type="() => Promise<void>">
  <Expandable title="Refetch the active workspace">
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the active workspace is refetched
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="updateWorkspace" type="(params: Partial<Workspace>) => Promise<Workspace>">
  <Expandable title="Update the active workspace">
    <ParamField path="params" type="Partial<Workspace>">
      Workspace fields to update
    </ParamField>
    <ResponseField name="returns" type="Promise<Workspace>">
      The updated workspace
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="getMembers" type="(options: { page?: number; limit?: number }) => Promise<{ data: WorkspaceMember[]; total: number }>">
  <Expandable title="Get workspace members">
    <ParamField path="options" type="{ page?: number; limit?: number }">
      Pagination options
    </ParamField>
    <ResponseField name="returns" type="Promise<{ data: WorkspaceMember[]; total: number }>">
      Paginated list of workspace members
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="getRoles" type="() => Promise<WorkspaceRole[]>">
  <Expandable title="Get workspace roles">
    <ResponseField name="returns" type="Promise<WorkspaceRole[]>">
      List of workspace roles
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="createRole" type="(name: string, permissions: string[]) => Promise<WorkspaceRole>">
  <Expandable title="Create a workspace role">
    <ParamField path="name" type="string">
      The role name
    </ParamField>
    <ParamField path="permissions" type="string[]">
      List of permissions for this role
    </ParamField>
    <ResponseField name="returns" type="Promise<WorkspaceRole>">
      The created role
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deleteRole" type="(role: WorkspaceRole) => Promise<void>">
  <Expandable title="Delete a workspace role">
    <ParamField path="role" type="WorkspaceRole">
      The role to delete
    </ParamField>
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the role is deleted
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="leave" type="() => Promise<void>">
  <Expandable title="Leave the active workspace">
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the user has left the workspace
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deleteWorkspace" type="() => Promise<void>">
  <Expandable title="Delete the active workspace">
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the workspace is deleted
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="removeMember" type="(memberId: string) => Promise<void>">
  <Expandable title="Remove a member from the workspace">
    <ParamField path="memberId" type="string">
      The member ID to remove
    </ParamField>
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the member is removed
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="addMemberRole" type="(memberId: string, roleId: string) => Promise<void>">
  <Expandable title="Add a role to a workspace member">
    <ParamField path="memberId" type="string">
      The member ID
    </ParamField>
    <ParamField path="roleId" type="string">
      The role ID to add
    </ParamField>
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the role is added
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="removeMemberRole" type="(memberId: string, roleId: string) => Promise<void>">
  <Expandable title="Remove a role from a workspace member">
    <ParamField path="memberId" type="string">
      The member ID
    </ParamField>
    <ParamField path="roleId" type="string">
      The role ID to remove
    </ParamField>
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the role is removed
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inviteMember" type="(params: { email: string, workspaceRoleId?: string }) => Promise<Invitation | undefined>">
  <Expandable title="Invite a member to the workspace">

    <ParamField path="params.email" type="string" required>
      Email address of the user to invite
    </ParamField>

    <ParamField path="params.workspaceRoleId" type="string">
      Optional workspace role ID to assign
    </ParamField>

    <ResponseField name="returns" type="Invitation | undefined">
      The invitation object if successful, or undefined if no active workspace
    </ResponseField>

  </Expandable>
</ResponseField>

## Return Types

<ResponseField name="Invitation">
  <Expandable title="Properties">

  <ResponseField name="id" type="string">
    Unique identifier for the invitation
  </ResponseField>

  <ResponseField name="email" type="string">
    Email address of the invited user
  </ResponseField>

  <ResponseField name="workspace_id" type="string">
    ID of the workspace this invitation belongs to
  </ResponseField>

  <ResponseField name="workspace_role" type="WorkspaceRole">
    Initial role assigned in the workspace
  </ResponseField>

  <ResponseField name="expires_at" type="string">
    ISO 8601 timestamp when the invitation expires
  </ResponseField>

  <ResponseField name="accepted_at" type="string | null">
    ISO 8601 timestamp when the invitation was accepted, or null if pending
  </ResponseField>

  <ResponseField name="created_at" type="string">
    ISO 8601 timestamp when the invitation was created
  </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="WorkspaceRole">
  <Expandable title="Properties">

  <ResponseField name="id" type="string">
    Unique identifier for the role
  </ResponseField>

  <ResponseField name="name" type="string">
    Display name of the role
  </ResponseField>

  <ResponseField name="permissions" type="string[]">
    List of permissions granted by this role
  </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="getInvitations" type="() => Promise<Invitation[]>">
  <Expandable title="Get workspace invitations">
    <ResponseField name="returns" type="Promise<Invitation[]>">
      List of workspace invitations
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="discardInvitation" type="(invitationId: string) => Promise<void>">
  <Expandable title="Discard a workspace invitation">
    <ParamField path="invitationId" type="string">
      The invitation ID to discard
    </ParamField>
    <ResponseField name="returns" type="Promise<void>">
      Resolves when the invitation is discarded
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resendInvitation" type="(invitationId: string) => Promise<Invitation>">
  <Expandable title="Resend a workspace invitation">
    <ParamField path="invitationId" type="string">
      The invitation ID to resend
    </ParamField>
    <ResponseField name="returns" type="Promise<Invitation>">
      The resent invitation
    </ResponseField>
  </Expandable>
</ResponseField>

## Examples

### Display Active Workspace

```tsx
import { useActiveWorkspace } from "@wacht/react-router";

function WorkspaceHeader() {
  const { activeWorkspace } = useActiveWorkspace();

  return (
    <header>
      <h1>{activeWorkspace?.name}</h1>
      <p>{activeWorkspace?.description}</p>
    </header>
  );
}
```

### Update Workspace

```tsx
import { useActiveWorkspace } from "@wacht/react-router";

function WorkspaceSettings() {
  const { updateWorkspace } = useActiveWorkspace();

  const handleUpdate = async () => {
    await updateWorkspace({
      name: "Updated Name",
      description: "Updated description",
    });
  };

  return <button onClick={handleUpdate}>Update Workspace</button>;
}
```

### Get Members

```tsx
import { useActiveWorkspace } from "@wacht/react-router";

function WorkspaceMembers() {
  const { getMembers } = useActiveWorkspace();
  const [members, setMembers] = useState([]);

  useEffect(() => {
    getMembers({ page: 1, limit: 10 }).then((result) => {
      setMembers(result.data);
    });
  }, [getMembers]);

  return (
    <ul>
      {members.map((member) => (
        <li key={member.id}>{member.user?.first_name}</li>
      ))}
    </ul>
  );
}
```

### Leave Workspace

```tsx
import { useActiveWorkspace } from "@wacht/react-router";

function LeaveWorkspaceButton() {
  const { leave } = useActiveWorkspace();

  return (
    <button onClick={() => leave()}>
      Leave Workspace
    </button>
  );
}
```

### Invite Member

```tsx
import { useActiveWorkspace } from "@wacht/react-router";

function InviteMember() {
  const { inviteMember, activeWorkspace } = useActiveWorkspace();

  const handleInvite = async (email: string) => {
    const invitation = await inviteMember({
      email,
      workspaceRoleId: "workspace_role_id_123",
    });

    if (invitation) {
      console.log("Invitation sent:", invitation.id);
    }
  };

  return (
    <button onClick={() => handleInvite("user@example.com")}>
      Invite to {activeWorkspace?.name}
    </button>
  );
}
```

## Related

- [useWorkspaceList](./use-workspace-list) - Workspace list management
- [useWorkspaceMemberships](./use-workspace-memberships) - Memberships list
- [OrganizationSwitcher](../components/organization-switcher) - Switcher component
